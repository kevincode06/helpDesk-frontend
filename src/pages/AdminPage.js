import { useEffect } from "react";

function AdminPage () {
    useEffect(() => {

    }, []);

    return (
        <div className="admin-page">
            <h2> ADmin Dashboard</h2>
            {/* Add Admin-specific UI: list tickets, states, actions */}
        </div>
    );
}

export default AdminPage;